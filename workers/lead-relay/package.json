{
  "name": "ctl-lead-relay",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev --env dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "schema": "wrangler d1 execute ctl-leads --remote --file=schema.sql",
    "schema:local": "wrangler d1 execute ctl-leads --local --env dev --file=schema.sql",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250801.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.90.0"
  }
}
